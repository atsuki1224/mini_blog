<div class="contents row">
  <div class="container">
    <%= form_for lyrics_path do |form| %>
      <h3>
        編集する
      </h3>
      <%= form.text_area :text, placeholder: "text" %>
      <%= form.submit "SEND" %>
    <% end %>
  </div>
</div>